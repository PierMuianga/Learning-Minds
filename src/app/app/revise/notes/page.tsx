import Link from "next/link"; import { SubjectHeader } from "@/components/app/subject-header"; import { units } from "@/modules/curriculum/catalogue";
export const metadata={title:"Revision Notes"};
export default function NotesPage(){return <><SubjectHeader title="Revision notes" description="Move through the curriculum in order, or return directly to the idea you need."/><div className="mx-auto max-w-5xl px-5 py-10 sm:px-9 lg:px-12">{units.map(unit=><section key={unit.id} className="mb-14"><div className="flex items-baseline gap-4 border-b border-ink-950/20 pb-3"><span className="text-xs font-bold tracking-[.18em] text-sky-500 uppercase">Unit {unit.order}</span><h2 className="font-display text-3xl">{unit.title}</h2></div><ol>{unit.topics.map(topic=><li key={topic.id} className="border-b border-ink-950/10"><Link href={topic.note?`/app/revise/notes/${topic.slug}`:"/app/revise/questions?topic="+topic.id} className="group grid grid-cols-[2.5rem_1fr_auto] items-center gap-3 py-6"><span className="font-display text-xl text-ink-500">{String(topic.order).padStart(2,"0")}</span><span><strong className="text-lg">{topic.title}</strong><span className="mt-1 block text-sm text-ink-500">{topic.summary}</span></span><span className="text-sm font-bold text-leaf-700">{topic.note?"Read notes →":"Practise →"}</span></Link></li>)}</ol></section>)}</div></>}
