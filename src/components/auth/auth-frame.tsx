import type { ReactNode } from "react";
import { Wordmark } from "@/components/brand/wordmark";
import { LearningPath } from "@/components/visuals/learning-path";

export function AuthFrame({ children, mode = "signup" }: { children: ReactNode; mode?: "login" | "signup" }) {
  const login = mode === "login";
  return <main className="min-h-screen bg-paper lg:grid lg:grid-cols-[minmax(0,1.06fr)_minmax(29rem,.94fr)]"><section className="diagram-grid-inverse relative hidden min-h-screen overflow-hidden bg-ink-950 p-10 text-paper lg:flex lg:flex-col xl:p-14"><Wordmark inverse /><div className="relative z-10 my-auto max-w-2xl"><p className="text-sm font-bold text-sun-500">{login ? "Pick up where you left off" : "Your personal learning map"}</p><h1 className="mt-4 font-display text-5xl leading-[.98] tracking-[-.04em] text-balance xl:text-6xl">{login ? <>Welcome back.<br/>Your progress is waiting.</> : <>Your learning,<br/>mapped around you.</>}</h1><p className="mt-5 max-w-md leading-7 text-paper/60">{login ? "Return to a space that remembers your direction." : "A clear, connected path through every subject you choose."}</p><div className="relative mt-7 max-w-xl rounded-lg border border-paper/12 bg-white/[.035] p-4"><LearningPath variant="dark" className="w-full"/><span className="absolute left-[25%] top-10 rounded-full bg-violet-500 px-3 py-1 text-xs font-bold">Maths</span><span className="absolute right-[17%] top-16 rounded-full bg-sky-500 px-3 py-1 text-xs font-bold">Physics</span><span className="absolute left-[44%] bottom-10 rounded-full bg-leaf-600 px-3 py-1 text-xs font-bold">Biology</span></div><div className="mt-4 flex gap-3"><div className="rounded-md border border-paper/12 bg-ink-950 px-4 py-3"><p className="text-xs text-paper/45">{login ? "Saved progress" : "Today"}</p><p className="mt-1 text-sm font-bold">{login ? "Ready when you are" : "2 lessons complete · +40 XP"}</p></div><div className="rounded-md border border-paper/12 bg-ink-950 px-4 py-3"><p className="text-xs text-paper/45">Current focus</p><p className="mt-1 text-sm font-bold">Algebra</p></div></div></div><p className="text-xs text-paper/40">Knowledge grows when every step connects.</p></section><section className="flex min-h-screen items-center px-5 py-9 sm:px-10 lg:px-14 xl:px-20"><div className="mx-auto w-full max-w-[29rem]"><div className="mb-12 lg:hidden"><Wordmark /></div>{children}</div></section></main>;
}
