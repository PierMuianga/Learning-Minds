import type { Question } from "@/modules/curriculum/types";
export function AnswerInput({question,value,onChange,disabled=false}:{question:Question;value:string;onChange:(v:string)=>void;disabled?:boolean}){if(question.type==="multiple_choice")return <fieldset disabled={disabled} className="grid gap-3"><legend className="sr-only">Select one answer</legend>{question.options?.map(option=><label key={option.id} className={`answer-option ${value===option.id?"answer-selected":""}`}><input type="radio" name={question.id} value={option.id} checked={value===option.id} onChange={e=>onChange(e.target.value)}/><span className="font-display text-xl text-ink-500">{option.id.toUpperCase()}</span><span>{option.label}</span></label>)}</fieldset>;if(question.type==="true_false")return <fieldset disabled={disabled} className="flex gap-3"><legend className="sr-only">Choose true or false</legend>{["true","false"].map(v=><label key={v} className={`answer-option flex-1 ${value===v?"answer-selected":""}`}><input type="radio" name={question.id} value={v} checked={value===v} onChange={e=>onChange(e.target.value)}/><span className="capitalize">{v}</span></label>)}</fieldset>;if(question.type==="structured_response")return <label className="block"><span className="mb-2 block text-sm font-bold">Show your working</span><textarea disabled={disabled} value={value} onChange={e=>onChange(e.target.value)} rows={6} className="answer-field" placeholder="Write each step clearly…"/></label>;return <label className="block"><span className="mb-2 block text-sm font-bold">Your answer</span><input disabled={disabled} type={question.type==="numerical"?"number":"text"} step="any" value={value} onChange={e=>onChange(e.target.value)} className="answer-field"/></label>}
