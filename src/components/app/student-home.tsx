import type { LearningContext } from "@/modules/onboarding/repository";
const greeting = () => { const hour = new Date().getUTCHours(); return hour < 12 ? "Good morning" : hour < 18 ? "Good afternoon" : "Good evening"; };
export function StudentHome({ data }: { data: LearningContext }) { return <div className="mx-auto max-w-6xl px-5 py-10 sm:px-9 sm:py-14 lg:px-12"><header className="border-b border-ink-950/10 pb-9"><p className="text-xs font-bold tracking-[.18em] text-leaf-700 uppercase">Your learning space</p><h1 className="mt-4 font-display text-4xl tracking-tight sm:text-6xl">{greeting()}, {data.profile.displayName}.</h1><p className="mt-4 text-ink-500">{data.curriculum} · {data.grade}</p></header><div className="mt-10 grid gap-10 xl:grid-cols-[1.5fr_.7fr]"><section className="relative min-h-80 overflow-hidden bg-ink-950 p-7 text-paper sm:p-10"><div className="relative z-10 max-w-xl"><p className="text-xs font-bold tracking-[.18em] text-sun-500 uppercase">The path ahead</p><h2 className="mt-5 font-display text-4xl leading-tight sm:text-5xl">Your learning journey starts here.</h2><p className="mt-5 max-w-md leading-7 text-paper/65">Your space is ready. The first lessons are being carefully prepared around the subjects you chose.</p></div><div aria-hidden className="absolute -right-24 -bottom-28 size-80 rounded-full border-[3rem] border-leaf-600/30"/></section><aside className="space-y-9"><section><p className="text-xs font-bold tracking-[.18em] text-ink-500 uppercase">Daily goal</p><div className="mt-3 flex items-baseline gap-2 border-b border-ink-950/10 pb-6"><strong className="font-display text-5xl font-normal">{data.goalMinutes}</strong><span className="text-ink-500">minutes</span></div></section><section><p className="text-xs font-bold tracking-[.18em] text-ink-500 uppercase">Your subjects</p><ul className="mt-3 divide-y divide-ink-950/10">{data.subjects.map((subject, index) => <li key={subject} className="flex items-center gap-4 py-3"><span className="font-display text-sm text-ink-500">0{index + 1}</span><span className="font-bold">{subject}</span></li>)}</ul></section></aside></div><section className="mt-14 grid gap-6 border-t border-ink-950/10 pt-8 sm:grid-cols-2"><div><p className="text-xs font-bold tracking-wider text-ink-500 uppercase">Learn</p><h3 className="mt-3 font-display text-2xl">Build understanding, step by step.</h3><p className="mt-2 text-sm leading-6 text-ink-500">Structured learning experiences will live here in the next product phase.</p></div><div><p className="text-xs font-bold tracking-wider text-ink-500 uppercase">Revise</p><h3 className="mt-3 font-display text-2xl">Return to what matters.</h3><p className="mt-2 text-sm leading-6 text-ink-500">Focused revision tools are the next part of your journey.</p></div></section></div>; }
