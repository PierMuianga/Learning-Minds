import type { LearningContext } from "@/modules/onboarding/repository";
import { LearningPath } from "@/components/visuals/learning-path";
export function TeacherHome({ data }: { data: LearningContext }) { return <div className="mx-auto max-w-7xl px-5 py-8 sm:px-9 lg:px-12 lg:py-11"><header className="grid gap-6 border-b border-ink-950/10 pb-8 md:grid-cols-[1fr_auto] md:items-end"><div><p className="text-sm font-bold text-leaf-700">Teacher overview</p><h1 className="mt-2 text-3xl font-bold tracking-tight sm:text-5xl">Welcome, {data.profile.displayName}.</h1><p className="mt-3 text-ink-500">A focused place to guide learning and see the path ahead.</p></div><div className="rounded-md border border-ink-950/10 bg-canvas px-5 py-3 md:text-right"><p className="text-xs text-ink-500">Curriculum</p><p className="mt-1 font-bold">{data.curriculum}</p></div></header><div className="mt-7 grid gap-6 lg:grid-cols-[.62fr_1.38fr]"><section className="rounded-lg border border-ink-950/10 bg-white p-6"><p className="text-sm font-bold text-ink-500">Teaching</p><ul className="mt-4">{data.subjects.map((subject, i) => <li key={subject} className="flex items-center justify-between border-b border-ink-950/10 py-4 last:border-0"><span className="font-bold">{subject}</span><span className="grid size-7 place-items-center rounded-full bg-leaf-100 text-xs text-leaf-700">{i + 1}</span></li>)}</ul></section><section className="diagram-grid-inverse relative min-h-[390px] overflow-hidden rounded-lg bg-ink-950 p-7 text-paper sm:p-9"><p className="text-sm font-bold text-sun-500">Teaching workspace</p><h2 className="mt-3 max-w-lg text-3xl font-bold leading-tight">Bring your learning community together.</h2><p className="mt-3 max-w-lg leading-7 text-paper/60">Classes, assignments and tests are planned for a future phase. Your curriculum and subjects are ready.</p><LearningPath variant="dark" compact className="absolute inset-x-4 bottom-8 w-[calc(100%-2rem)] opacity-80"/></section></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[["Classes","Organise learners into clear groups"],["Assignments","Set purposeful work"],["Tests","Check understanding"]].map(([title,copy], i)=><div key={title} className="rounded-lg border border-ink-950/10 bg-canvas p-5"><div className="flex items-center justify-between"><strong>{title}</strong><span className="text-xs text-ink-500">0{i+1}</span></div><p className="mt-2 text-sm text-ink-500">{copy}</p><p className="mt-5 text-xs font-bold text-leaf-700">Planned for a future phase</p></div>)}</div></div>; }
