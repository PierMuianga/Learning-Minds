import type { LearningContext } from "@/modules/onboarding/repository";
export function TeacherHome({ data }: { data: LearningContext }) { return <div className="mx-auto max-w-6xl px-5 py-10 sm:px-9 sm:py-14 lg:px-12"><header className="grid gap-7 border-b border-ink-950/10 pb-10 md:grid-cols-[1fr_auto] md:items-end"><div><p className="text-xs font-bold tracking-[.18em] text-leaf-700 uppercase">Overview</p><h1 className="mt-4 font-display text-4xl tracking-tight sm:text-6xl">Welcome, {data.profile.displayName}.</h1><p className="mt-4 max-w-xl leading-7 text-ink-500">Your teaching workspace is ready for the work ahead.</p></div><div className="md:text-right"><p className="text-xs font-bold tracking-wider text-ink-500 uppercase">Curriculum</p><p className="mt-2 font-display text-xl">{data.curriculum}</p></div></header><div className="mt-10 grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><section><p className="text-xs font-bold tracking-wider text-ink-500 uppercase">Subjects taught</p><ul className="mt-5 border-t border-ink-950/10">{data.subjects.map((subject, i) => <li key={subject} className="flex justify-between border-b border-ink-950/10 py-4"><span className="font-bold">{subject}</span><span className="font-display text-ink-500">0{i + 1}</span></li>)}</ul></section><section className="bg-ink-950 p-8 text-paper sm:p-10"><p className="text-xs font-bold tracking-[.18em] text-sun-500 uppercase">A calm place to begin</p><h2 className="mt-4 max-w-lg font-display text-4xl leading-tight">Bring your learning community together.</h2><p className="mt-5 max-w-lg leading-7 text-paper/60">Classes, assignments and tests will arrive in a future phase. Your curriculum and subjects are already in place.</p><div className="mt-10 grid grid-cols-3 gap-px bg-paper/15">{["Classes", "Assignments", "Tests"].map((item) => <div key={item} className="bg-ink-950 py-4 text-center text-xs font-bold text-paper/70">{item}</div>)}</div></section></div></div>; }
